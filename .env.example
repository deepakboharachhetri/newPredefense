# Environment variables for API configuration

# Docker environment (set to true when running in Docker)
VITE_DOCKER_ENV=false

# API Host (use 'localhost' for local, or Docker container name/IP)
VITE_API_HOST=localhost

# API Port (default: 8000)
VITE_API_PORT=8000

# For Docker deployments:
# VITE_DOCKER_ENV=true
# VITE_API_HOST=predefense-api (or container IP)
